# Optional deliverable status

Scope: the public repository inspected for this revision. Separately prepared local packages and recordings are outside this verification.

| Step | Verified public content | Limit of the claim |
|---|---|---|
| 8 Deployment and MLOps | Saved model, command-line prediction, pinned Python requirements, tests, CI configuration and version/hash records | A complete Flask/FastAPI/Dash app, Docker package, deployment guide and deployment demo are not included here. Step 8 local web deployment is not claimed complete by this release. |
| 9 Generative AI | AI-assisted saved draft, aggregate sources, numeric validation, tests and recorded replay demonstration | The program replays saved content. It does not call a live LLM. Investigator narrative review remains pending. |

To include a separately prepared Step 8 package, verify the app starts, accepts a synthetic input, returns the saved-model prediction, and has working installation, monitoring and rollback instructions plus a matching demo. Preserve the fitted model/threshold contract.

To claim a live GenAI application, include the implemented code, actual model/runtime configuration, synthetic or aggregate-only example and a recording of successful live generation. Retrieval-only behavior, failure handling and saved-text playback must be labeled accurately. Record limitations and evaluate factual consistency. Do not upload credentials, private conversations, raw patient records or unreviewed recordings.

The optional features do not alter the locked random-forest results. New verification evidence should be added only after it exists.
